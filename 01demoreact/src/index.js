import React from 'react'; //take all the references
import ReactDOM from 'react-dom/client'; //implementation of react on web 
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); //kud ka DOM banata hai aur compare krta hai
root.render(
    <App />
);
